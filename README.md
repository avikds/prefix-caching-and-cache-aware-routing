# prefix-caching-and-cache-aware-routing
Chapter 5.3 of Inference Engineering as a working system. Build the block-hash prefix cache that inference engines use to reuse KV state across requests: chained hashes over fixed-size token blocks, longest-prefix lookup, LRU eviction under a block budget, and hit-rate accounting. Price a hit in time to first token and in bytes, then generate the…
